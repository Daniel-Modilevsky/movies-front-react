import NavBarComponent from "../navbar/navbar"

const ProfilePage =()=>{
    return (
        <div>
            <NavBarComponent/>
            <main>
                <header>
                    <h1>Profile Page</h1>
                </header>
            </main>
        </div>
    );
}

export default ProfilePage;