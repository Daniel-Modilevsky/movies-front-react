import NavBarComponent from "../navbar/navbar"

const MoviePage =()=>{
    return (
        <div>
            <NavBarComponent/>
            <main>
                <header>
                    <h1>Movie Page</h1>
                </header>
            </main>
        </div>
    );
}

export default MoviePage;