import NavBarComponent from "../navbar/navbar"

const ListsPage =()=>{
    return (
        <div>
            <NavBarComponent/>
            <main>
                <header>
                    <h1>Lists Page</h1>
                </header>
            </main>
        </div>
    );
}

export default ListsPage;