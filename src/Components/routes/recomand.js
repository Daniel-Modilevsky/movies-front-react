import NavBarComponent from "../navbar/navbar"

const RecomandPage =()=>{
    return (
        <div>
        <NavBarComponent/>
        <main>
            <header>
                <h1>Recomand Page</h1>
            </header>
        </main>
    </div>
    );
}

export default RecomandPage;